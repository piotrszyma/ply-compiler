( Rozklad liczby na czynniki pierwsze )
VAR
    a b c
BEGIN
    a := 10 * 10;

    IF a <> 100 THEN
        WRITE 1;
    ENDIF

    a := 10 * 0;

    IF a <> 0 THEN
        WRITE 1;
    ENDIF

    a := 10 * 11;

    IF a <> 110 THEN
        WRITE 1;
    ENDIF

END